# APCS 核心运行依赖（CI / 纯 CPU 即可跑通全部 task 与测试）
#
# 核心依赖只覆盖离线/CPU 路径；真实 HF 数据与 GPU 模型路径请安装 extra：
#   pip install -e ".[gpu]"
# 论文 Figure 渲染请安装：      pip install -e ".[figures]"
#
# 推荐安装方式（等价且会注册 `apcs` 命令）：
#   pip install -e .
numpy>=1.26
scipy>=1.11
pyyaml>=6.0
