# APCS 核心运行依赖（CI / 纯 CPU 即可跑通全部 task 与测试）
#
# 说明：当前代码库为**离线仿真**实现，不加载任何模型权重或数据集，
# 因此 torch / transformers / datasets 不在此文件中。
# 真实 GPU 实验请安装 extra：  pip install -e ".[gpu]"
# 论文 Figure 渲染请安装：      pip install -e ".[figures]"
#
# 推荐安装方式（等价且会注册 `apcs` 命令）：
#   pip install -e .
numpy>=1.26
scipy>=1.11
pyyaml>=6.0
