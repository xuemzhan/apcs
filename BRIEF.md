# BRIEF

让大模型在用户数据上先跑一遍，把生成的 KV 缓存存下来，经过一个映射器迁到小模型，小模型冷启动时就不用重新 prefill。这条"缓存翻译"路线，在我们的实验里走不通：冻结的小模型加上严格的零 re-prefill，能力迁移做不到。教师的优势留在它的参数里，FFN 那部分权重上，不在这份缓存里。

三十次真实 GPU 运行把原因拆开了。注入机制本身没有损失：把学生自己的缓存原样走一遍管线，和学生自 prefill 逐样本完全等价，logit 余弦 1.000。映射器是下一个被排除的：ridge、affine、逐层 affine、task-aware、RAT、MLP 六族全都低于替换级，而且换 seed、加校准数据、加非线性都动不了这个结果，说明瓶颈根本不在映射器家族。最后 oracle 探针直接回答了"就算映射器完美，学生能不能用上教师缓存里的东西"，答案是不能。把教师内容混进学生缓存，比例越高答案越差；重建到接近原生流畅度（PPL 23.7 vs 21.2），准确率还是停在 0.267，离学生的 0.500 差得远。

这篇东西的价值不在结论本身，而在查法。恒等对照、oracle 上界探针、gold 概率配置信区间、固定评估集的规模阶梯，这几样加起来，让缓存翻译的每一层声明都有了能一跑就知道真假的检查单。MoT、C2C 那些路线以后想报数，得先过这一关。

两个还能用的边角：弱学生（0.6B 对 4B）能拿到"不重算就替换"的平局，准确率和自 prefill 统计上没差别；顶层三分之一注入无害，虽然花在那里也换不来什么。
